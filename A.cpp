#include <iostream>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);

    freopen("aplusb.in", "r", stdin);
    freopen("aplusb.out", "w", stdout);

    int a, b;
    cin>>a>>b;
    cout<<a + b;

    return 0;
}
